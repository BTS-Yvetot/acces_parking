Afin de pouvoir utiliser "git for windows" dans le lycée, il faut configurer le proxy. Cela ne peut se faire
qu'en ligne de commande. 
Il faut donc lancer Git Bash et entrer la ligne suivante : 
git config --global http.proxy 172.19.200.1:3128
On peut alors lancer Git GUI